name: date
description: Hour, Date, Month implementations for Dart and other time related utilities.
version: 3.0.6

environment:
  sdk: ^3.0.2

dependencies:
  intl: ^0.19.0
  petitparser: ^6.0.0
  timezone: ^0.9.2

dev_dependencies:
  lints: ^3.0.0
  test: ^1.16.5
